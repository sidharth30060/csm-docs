# .github/workflows/dummy-action.yaml
name: Dummy Action

on:
  workflow_dispatch:  # Enables manual trigger from GitHub UI

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Say Hello
        run: echo "Hello, World! Manually triggered!"
